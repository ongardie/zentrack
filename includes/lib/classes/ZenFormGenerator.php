<? /* -*- Mode: C; c-basic-indent: 3; indent-tabs-mode: nil -*- ex: set tabstop=3 expandtab: */ 

/** @package Utils */
class ZenFormGenerator extends Zen {

  // constructor - takes table, ZenTemplate(layout)

  // merge - merges fields with another ZenFormGenerator, merged form layout props are discarded

  // addField - adds a new field to the form

  // alterField - edits properties for db field

  // setValue - sets a field value

  // values - sets many field values

  // render - outputs html for this form

}

?>
