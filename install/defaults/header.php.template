<? /* -*- Mode: C; c-basic-indent: 3; indent-tabs-mode: nil -*- ex: set tabstop=3 expandtab: */ 

  // this variable must match the location of the includes/ directory
  $inc_dir = "{inc_dir}";

  // this variable must match the full file path location of your zen.ini file
  $ini_file = "$inc_dir/config/zen.ini";

  // these lines do not need to be edited
  include("$inc_dir/lib/inc/global.php");

?>
